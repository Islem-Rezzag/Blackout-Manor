datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model WorkspaceHealth {
  id        String   @id @default(cuid())
  name      String   @unique
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}
